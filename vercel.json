{
  "version": 2,
  "builds": [
    { "src": "services/**/*.jsx", "use": "@vercel/node" },
    { "src": "src/**/*", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/services/api.jsx" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
